const hangman = require('../hangman');

describe('playHangman', () => {
  test.todo(
    'should print success message and end when a completed word has be been revealed',
  );

  test.todo('should print a failure message and and when you run out of strikes');

  test.todo('should continue play if there was no win or loss');
});
